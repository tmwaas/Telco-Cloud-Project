{{- if .Values.openshift.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata: { name: core-sim }
spec:
  to: { kind: Service, name: core-sim }
  port: { targetPort: http }
  # port: { targetPort: 80 }
  tls: { termination: edge }
{{- end }}
