apiVersion: v1
kind: LimitRange
metadata: { name: default-limits, namespace: ns-ran }
spec:
  limits:
  - type: Container
    default: { cpu: "500m", memory: 512Mi }
    defaultRequest: { cpu: "100m", memory: 128Mi }
---
apiVersion: v1
kind: LimitRange
metadata: { name: default-limits, namespace: ns-core }
spec:
  limits:
  - type: Container
    default: { cpu: "500m", memory: 512Mi }
    defaultRequest: { cpu: "100m", memory: 128Mi }
